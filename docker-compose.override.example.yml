# Copie para docker-compose.override.yml no desenvolvimento local
# para publicar Postgres, API e Web no host.
services:
  postgres:
    ports:
      - "5432:5432"
  api:
    ports:
      - "3001:3001"
  web:
    ports:
      - "8080:80"
